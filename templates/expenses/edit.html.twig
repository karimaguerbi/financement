{% extends 'base.html.twig' %}

{% block title %}Edit Expenses{% endblock %}

{% block body %}
    <h1>Edit Expenses</h1>

    {{ include('expenses/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('expenses_index') }}"><button class="btn btn-lg btn-primary" type="submit" >back to list</button></a>

    {{ include('expenses/_delete_form.html.twig') }}
{% endblock %}
