{% extends 'base.html.twig' %}

{% block title %}Financement{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
 {% if is_granted('IS_AUTHENTICATED_FULLY') %}
 <h1>Hello  {{app.user.username}} ✅</h1>
<h4><font color="#0000FF">La somme Expenses est :</font><font color="#800080"> {{ somme|number_format(3, ',', ' ')}} DT </font></h4><br>
<h4><font color="#0000FF">La somme Income est : </font><font color="#FFA500">{{ total|number_format(3, ',', ' ')}} DT</font></h4><br>

 {% if total>somme%}
 <h4><font color="#0000FF"> it's OK L'Intervale est de :</font> <font color="#00FF00"> {{total}}-{{somme}}</font></h4>
  {% else %}
 <h4><font color="#0000FF"> is NOT OK L'Intervale est de :</font><font color="red"> {{total}}-{{somme}}</font></h4>
{% endif %} 
  {% else %}
    <h1>Hello AbdelKarim ✅</h1>

  <a href="{{path('app_login')}}"><img src="{{asset('img/UGTT.jpg')}}" alt="ugtt"/></a>

    {% endif %} 
</div>
{% endblock %}
